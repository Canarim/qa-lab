/**
 * Primeira função do laboratório. Existe só pra provar que o ambiente
 * (TypeScript + Vitest) está funcionando — o "smoke test".
 * Na próxima sessão a gente substitui isso por código de verdade.
 */
export function saudacao(nome: string): string {
  return `Fala, ${nome}! Ambiente verde. 🥋`;
}
